n = int(input())
arr = list(map(int, input().split()))

freq = {}

for num in arr:
    freq[num] = freq.get(num, 0) + 1

unique_sum = 0
for num, count in freq.items():
    if count == 1:
        unique_sum += num

print(unique_sum)
